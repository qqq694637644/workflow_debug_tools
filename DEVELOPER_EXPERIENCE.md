# Workflow 开发者体验路线图

这个文档记录的是 Workflow 作为一门“真可用”的脚本语言，还需要补齐哪些能力。这里不讨论语法本身是否像 Lua，而是讨论它在真实工程里能不能像成熟语言一样写、查、跑、调、观测。

## 目标

Workflow 已经具备语言核心、编译链路、运行时和调试器的基础能力，但要成为一门适合长期使用的脚本语言，还需要补上一层开发者体验能力。

重点不是“能不能执行”，而是下面这些问题：

- 能不能在 VSCode 里高效编写
- 能不能快速定位错误
- 能不能像 Lua 一样低成本试验和调试
- 能不能清楚地看见协程、状态机、绑定和反射调用的运行过程
- 能不能把脚本工程化，而不是只能手工拼接和运行

## 为什么要补这些能力

Lua 在 VSCode 里的体验之所以普遍不错，不只是因为语法简单，而是因为配套能力足够完整：

- 语法高亮明确
- 自动补全和跳转定义可用
- 断点和变量查看比较直观
- 脚本改动后可以很快重新运行
- 出错时通常可以迅速定位到行号和调用栈

Workflow 的目标比 Lua 更高，因为它还承担了：

- 强类型语义检查
- 和 C++ 反射互操作
- 程序集序列化和加载
- 生成 C++ 代码
- 协程和状态机的可观测执行

所以，Workflow 不能只“像 Lua 一样能写”，还要“比 Lua 更适合工程化使用”。

## 需要补齐的能力

### 1. VSCode 语言服务

这是最基础的一层，也是最直接影响日常使用的一层。

需要具备：

- 语法高亮
- 自动补全
- 跳转定义
- 查找引用
- 重命名符号
- 悬停显示类型和文档
- 实时语义诊断
- 符号大纲
- 代码折叠
- 面包屑导航

对比 Lua：

- Lua 更依赖“轻量和宽松”，所以语言服务只要够顺手就能用
- Workflow 更依赖“类型和作用域正确性”，所以语言服务必须更强

### 2. 调试器集成

Workflow 已经有运行时调试能力，但在编辑器里还需要完整的调试体验。

需要具备：

- 断点
- 条件断点
- 单步进入
- 单步跳过
- 单步跳出
- 调用栈查看
- 局部变量查看
- 全局变量查看
- 监视表达式
- 异常断点
- 运行到光标

Workflow 还需要额外照顾这些语言特性：

- 协程挂起和恢复点
- 状态机状态迁移
- `bind(...)` 表达式的依赖变化
- 反射调用实际落到的宿主类型和成员

对比 Lua：

- Lua 调试主要关心当前执行到哪里
- Workflow 调试还要关心“为什么会跳到这里”“这个值从哪来”“这个状态为什么被触发”

### 3. 可观测性

这是 Workflow 要真正“可用”时非常关键的一层。

需要具备：

- 运行日志
- 结构化 Trace
- 协程生命周期追踪
- 状态机迁移记录
- 绑定表达式依赖追踪
- 反射调用轨迹
- 简单的性能采样
- 执行时间和热点统计

如果没有这些能力，复杂脚本一旦出问题，就会变成“能运行，但看不懂为什么这样运行”。

### 4. 工程化任务流

脚本语言要进项目，不能只靠手工点开文件执行。

需要具备：

- 工作区级项目配置
- 多模块依赖管理
- 一键编译
- 一键运行
- 一键生成 C++ 代码
- 一键执行测试
- 产物和源码的映射关系
- 生成结果的可追踪性

对比 Lua：

- Lua 往往更偏“脚本文件直接跑”
- Workflow 更像“有编译阶段的语言”，所以任务流必须工程化

### 5. 错误信息质量

Workflow 的语义比 Lua 更严格，所以错误信息必须足够清晰。

需要具备：

- 精确行列定位
- 明确指出错误发生的语义阶段
- 说明是名称解析错误、类型错误、作用域错误，还是状态机或协程规则错误
- 给出期望类型和实际类型
- 给出尽量可执行的修复建议

如果错误信息只告诉你“失败了”，那强类型和静态分析的优势会被大幅抵消。

### 6. C++ 互操作可视化

Workflow 的核心价值之一就是和 C++ 深度联动，所以编辑器和运行工具最好能把映射关系讲清楚。

需要具备：

- Workflow 类型到 C++ 类型的映射查看
- 反射成员的来源查看
- 生成代码和手写代码的边界展示
- 代码生成规则的可追踪性
- 哪些成员会影响导出、序列化和运行时的可见性

这点在 Lua 场景里通常不是重点，但对 Workflow 是重点。

### 7. 快速反馈

如果每次修改都要经历很长的编译和加载过程，脚本语言的优势会被明显削弱。

需要具备：

- 增量编译
- 缓存解析结果
- 缓存语义分析结果
- 程序集快速加载
- 热重载或近似热重载
- 失败后保留上一次可运行版本

对比 Lua：

- Lua 的强项之一是修改后反馈快
- Workflow 需要在“强类型”和“快反馈”之间找到平衡

## 推荐实现顺序

建议按下面的顺序补：

1. VSCode 语言服务
2. 调试器集成
3. 错误信息质量
4. 可观测性
5. 工程化任务流
6. C++ 互操作可视化
7. 快速反馈和增量加载

这样做的原因是：

- 前三项直接影响“能不能写”
- 中间两项直接影响“能不能看懂和定位问题”
- 后两项决定“能不能长期使用”

## 目前的判断

Workflow 已经不是玩具语言，它更接近一套面向 C++ 生态的脚本平台。

如果只看语言语法，它可能会让人联想到 Lua。
但如果看开发体验目标，它应该追求的是：

- 比 Lua 更强的静态分析
- 比 Lua 更清晰的类型和错误信息
- 比 Lua 更适合工程化的调试和观测
- 比 Lua 更容易进入大型 C++ 项目

## 后续说明

后续如果逐步实现这些能力，建议按本文件的顺序更新：

- 每完成一项，就补充对应的使用说明
- 每个能力都保留“如何在 VSCode 中使用”的入口
- 每个能力都保留“和 Lua 的差异说明”
- 每个能力都保留“当前已支持/未支持”的边界

